'use client';
import { useState } from 'react';
export default function TrackOrderPage(){
 const [orderNumber,setOrderNumber]=useState(''),[phone,setPhone]=useState(''),[data,setData]=useState<any>(null),[error,setError]=useState(''),[loading,setLoading]=useState(false);
 async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');setData(null);const r=await fetch('/api/track-order',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({orderNumber,phone})});const j=await r.json();setLoading(false);if(!r.ok){setError(j.error||'Unable to find order.');return}setData(j.order);}
 return <main className="min-h-screen bg-slate-50 px-4 py-10"><div className="mx-auto max-w-2xl"><div className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200 md:p-8"><h1 className="text-3xl font-black">Track your order</h1><p className="mt-2 text-slate-500">Enter your order number and phone number.</p><form onSubmit={submit} className="mt-6 grid gap-3"><input value={orderNumber} onChange={e=>setOrderNumber(e.target.value)} placeholder="ORD-2026-0001" className="rounded-xl border px-4 py-3"/><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="10-digit phone number" inputMode="tel" className="rounded-xl border px-4 py-3"/><button disabled={loading} className="rounded-xl bg-slate-950 px-4 py-3 font-bold text-white disabled:opacity-50">{loading?'Checking…':'Track order'}</button></form>{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}{data&&<section className="mt-7"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-sm text-slate-500">Order</p><h2 className="text-xl font-black">{data.orderNumber}</h2></div><span className="rounded-full bg-slate-100 px-3 py-1 text-sm font-bold">{data.status}</span></div>{data.trackingNumber&&<p className="mt-4 text-sm"><b>Tracking:</b> {data.courierName||'Courier'} · {data.trackingNumber} {data.trackingUrl&&<a className="font-bold underline" href={data.trackingUrl} target="_blank" rel="noreferrer">Track shipment</a>}</p>}<div className="mt-6 space-y-3">{data.history.map((h:any)=><div key={h.createdAt+h.newStatus} className="rounded-xl border p-4"><b>{h.newStatus}</b><p className="text-xs text-slate-500">{new Date(h.createdAt).toLocaleString('en-IN')} · {h.note||'Status updated'}</p></div>)}</div></section>}</div></div></main>;
}