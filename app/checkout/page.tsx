'use client';
import { FormEvent, useState } from 'react'; import { useRouter } from 'next/navigation'; import Link from 'next/link'; import { useCart } from '../../components/cart-provider';
export default function Checkout(){ const {items,subtotal}=useCart(); const router=useRouter(); const [loading,setLoading]=useState(false); const [error,setError]=useState('');
async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const form=new FormData(e.currentTarget);const body={customerName:String(form.get('customerName')||''),email:String(form.get('email')||''),phone:String(form.get('phone')||''),addressLine1:String(form.get('addressLine1')||''),addressLine2:String(form.get('addressLine2')||''),landmark:String(form.get('landmark')||''),city:String(form.get('city')||''),district:String(form.get('district')||''),state:String(form.get('state')||''),pinCode:String(form.get('pinCode')||''),items:items.map(i=>({productId:i.productId,quantity:i.quantity}))};try{const r=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await r.json();if(!r.ok)throw new Error(data.error||'Checkout failed');router.push(`/payment/${encodeURIComponent(data.orderNumber)}`);}catch(err){setError(err instanceof Error?err.message:'Unable to create order.')}finally{setLoading(false)}}
if(!items.length)return <main className="container py-16 text-center"><h1 className="text-2xl font-black">Your cart is empty</h1><Link href="/products" className="mt-5 inline-block rounded-xl bg-slate-900 px-6 py-3 font-bold text-white">Shop products</Link></main>;
return <main className="container py-8"><Link href="/cart" className="font-semibold">← Back to cart</Link><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_360px]"><form onSubmit={submit} className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-100"><h1 className="text-3xl font-black">Checkout</h1><div className="mt-7 grid gap-4 sm:grid-cols-2"><label className="sm:col-span-2 text-sm font-semibold">Full name<input name="customerName" required minLength={2} maxLength={100} className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Mobile number<input name="phone" required inputMode="numeric" pattern="[6-9][0-9]{9}" maxLength={10} className="mt-2 w-full rounded-xl border p-3" placeholder="10-digit mobile"/></label><label className="text-sm font-semibold">Email (optional)<input name="email" type="email" className="mt-2 w-full rounded-xl border p-3"/></label><label className="sm:col-span-2 text-sm font-semibold">Address<input name="addressLine1" required minLength={5} maxLength={200} className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Apartment / area<input name="addressLine2" maxLength={200} className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Landmark<input name="landmark" maxLength={120} className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">City<input name="city" required className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">District<input name="district" required className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">State<input name="state" required className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">PIN code<input name="pinCode" required inputMode="numeric" pattern="[0-9]{6}" maxLength={6} className="mt-2 w-full rounded-xl border p-3"/></label></div>{error&&<p className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="mt-7 w-full rounded-xl bg-slate-900 px-6 py-4 font-bold text-white disabled:opacity-50">{loading?'Creating order…':'Continue to payment'}</button></form><aside className="h-fit rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-100"><h2 className="font-bold">Order summary</h2>{items.map(i=><div key={i.productId} className="mt-3 flex justify-between gap-3 text-sm"><span>{i.name} × {i.quantity}</span><span>₹{(i.price*i.quantity).toLocaleString('en-IN',{minimumFractionDigits:2})}</span></div>)}<div className="mt-5 flex justify-between border-t pt-4"><span>Subtotal</span><strong>₹{subtotal.toLocaleString('en-IN',{minimumFractionDigits:2})</strong></div><p className="mt-2 text-xs text-slate-500">Final delivery and total are calculated again on the server.</p></aside></div></main>}
