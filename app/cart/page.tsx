'use client';
import Link from 'next/link'; import { useCart } from '../../components/cart-provider';
export default function CartPage(){ const {items,setQuantity,remove,subtotal}=useCart(); return <main className="container py-8"><div className="flex items-center justify-between"><Link href="/" className="text-2xl font-black">Zenvora</Link><Link href="/products" className="font-semibold">Continue shopping</Link></div><h1 className="mt-10 text-3xl font-black">Your cart</h1>{!items.length?<div className="py-20 text-center"><p className="text-slate-500">Your cart is empty.</p><Link href="/products" className="mt-5 inline-block rounded-xl bg-slate-900 px-6 py-3 font-bold text-white">Shop products</Link></div>:<div className="mt-8 grid gap-8 lg:grid-cols-[1fr_360px]"><section className="space-y-4">{items.map(item=><div key={item.productId} className="flex gap-4 rounded-2xl bg-white p-4 shadow-sm ring-1 ring-slate-100"><div className="h-24 w-24 shrink-0 overflow-hidden rounded-xl bg-slate-100">{item.image&&<img src={item.image} alt="" className="h-full w-full object-cover"/>}</div><div className="min-w-0 flex-1"><h2 className="font-bold">{item.name}</h2><p className="mt-1 font-black">₹{item.price.toLocaleString('en-IN',{minimumFractionDigits:2})}</p><div className="mt-3 flex items-center gap-3"><button onClick={()=>setQuantity(item.productId,item.quantity-1)} className="h-9 w-9 rounded-lg border">−</button><span>{item.quantity}</span><button onClick={()=>setQuantity(item.productId,item.quantity+1)} className="h-9 w-9 rounded-lg border">+</button><button onClick={()=>remove(item.productId)} className="ml-3 text-sm font-semibold text-red-600">Remove</button></div></div></div>)}</section><aside className="h-fit rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-100"><h2 className="text-xl font-bold">Summary</h2><div className="mt-5 flex justify-between"><span>Subtotal</span><strong>₹{subtotal.toLocaleString('en-IN',{minimumFractionDigits:2})}</strong></div><p className="mt-3 text-sm text-slate-500">Delivery is calculated securely at checkout.</p><Link href="/checkout" className="mt-6 block rounded-xl bg-slate-900 px-6 py-4 text-center font-bold text-white">Proceed to checkout</Link></aside></div>}</main> }
