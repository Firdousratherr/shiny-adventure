import {NextResponse} from 'next/server';import {auth} from '../../../auth';import {db} from '../../../lib/db';
export async function GET(req:Request){const id=new URL(req.url).searchParams.get('productId')||'';if(!id)return NextResponse.json({error:'productId required'},{status:400});const reviews=await db.productReview.findMany({where:{productId:id,status:'APPROVED'},orderBy:{createdAt:'desc'},take:50,select:{id:true,rating:true,title:true,body:true,customerName:true,verified:true,createdAt:true}});return NextResponse.json({reviews})}
export async function POST(req:Request){const s=await auth();if(s?.user?.role!=='customer'||!s.user.email)return NextResponse.json({error:'Customer login required.'},{status:401});const b=await req.json().catch(()=>({}));const rating=Number(b.rating);if(!b.productId||rating<1||rating>5||!Number.isInteger(rating))return NextResponse.json({error:'Valid product and rating are required.'},{status:400});const customer=await db.customerUser.findUnique({where:{email:s.user.email},select:{id:true,name:true,email:true}});if(!customer)return NextResponse.json({error:'Customer not found.'},{status:404});const review=await db.productReview.create({data:{productId:b.productId,customerId:customer.id,customerName:customer.name,customerEmail:customer.email,orderId:typeof b.orderId==='string'?b.orderId:null,rating,title:typeof b.title==='string'?b.title.trim():null,body:typeof b.body==='string'?b.body.trim():null,verified:false}});return NextResponse.json({review},{status:201})}