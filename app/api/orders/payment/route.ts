import { NextResponse } from 'next/server'; import { db } from '../../../../lib/db'; import { paymentSchema } from '../../../../lib/validation';
export async function POST(request:Request){try{const fd=await request.formData();const parsed=paymentSchema.safeParse({orderNumber:String(fd.get('orderNumber')||''),upiTransactionId:String(fd.get('upiTransactionId')||'')});if(!parsed.success)return NextResponse.json({error:'Invalid payment details.'},{status:400});const file=fd.get('screenshot');if(!(file instanceof File)||!['image/jpeg','image/png','image/webp'].includes(file.type)||file.size<1||file.size>5*1024*1024)return NextResponse.json({error:'Invalid screenshot. Use JPG, PNG or WebP up to 5 MB.'},{status:400});const order=await db.order.findUnique({where:{orderNumber:parsed.data.orderNumber},select:{id:true,status:true}});if(!order)return NextResponse.json({error:'Order not found.'},{status:404});if(order.status!=='PAYMENT_PENDING')return NextResponse.json({error:'This order is not awaiting payment.'},{status:409});const duplicate=await db.order.findUnique({where:{upiTransactionId:parsed.data.upiTransactionId},select:{id:true}});if(duplicate)return NextResponse.json({error:'This UTR has already been submitted.'},{status:409});return NextResponse.json({error:'Screenshot storage is not configured yet. Configure Vercel Blob before accepting payment proofs.'},{status:503});}catch(error){console.error('payment submission failed',error);return NextResponse.json({error:'Unable to submit payment details right now.'},{status:500});}}
