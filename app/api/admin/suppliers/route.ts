import { NextResponse } from 'next/server'; import { requireAdminPermission } from '../../../../lib/admin-access'; import { db } from '../../../../lib/db';
export async function GET(){const a=await requireAdminPermission('marketplaces');if(!a)return NextResponse.json({error:'Unauthorized'},{status:401});return NextResponse.json({suppliers:await db.supplier.findMany({orderBy:{name:'asc'},include:{_count:{select:{products:true}}})});}
export async function POST(req:Request){const a=await requireAdminPermission('marketplaces');if(!a)return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json().catch(()=>({}));const name=String(b.name||'').trim();if(!name)return NextResponse.json({error:'Supplier name is required.'},{status:400});try{const s=await db.supplier.create({data:{name,website:b.website||null,contactName:b.contactName||null,contactEmail:b.contactEmail||null,contactPhone:b.contactPhone||null,notes:b.notes||null}});return NextResponse.json({supplier:s})}catch{return NextResponse.json({error:'Supplier already exists or could not be created.'},{status:409})}}
export async function PATCH(req:Request){const a=await requireAdminPermission('marketplaces');if(!a)return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json().catch(()=>({}));if(!b.id)return NextResponse.json({error:'ID required.'},{status:400});const s=await db.supplier.update({where:{id:b.id},data:{name:b.name,website:b.website||null,contactName:b.contactName||null,contactEmail:b.contactEmail||null,contactPhone:b.contactPhone||null,notes:b.notes||null,status:b.status||'ACTIVE'}});return NextResponse.json({supplier:s})}