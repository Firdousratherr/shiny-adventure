import {NextResponse} from 'next/server';import {requireAdminPermission} from '../../../../lib/admin-access';import {db} from '../../../../lib/db';
export async function GET(){const a=await requireAdminPermission('orders');if(!a)return NextResponse.json({error:'Unauthorized'},{status:401});return NextResponse.json({goals:await db.businessGoal.findMany({orderBy:{periodEnd:'desc'}})})}
export async function POST(req:Request){const a=await requireAdminPermission('settings');if(!a)return NextResponse.json({error:'Settings permission required.'},{status:403});const b=await req.json();const g=await db.businessGoal.create({data:{name:String(b.name),metric:String(b.metric),target:b.target,periodStart:new Date(b.periodStart),periodEnd:new Date(b.periodEnd)}});return NextResponse.json({goal:g})}