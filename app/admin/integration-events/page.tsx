import Link from 'next/link';import {redirect} from 'next/navigation';import AdminNav from '../../../components/admin-nav';import {requireAdminPermission} from '../../../lib/admin-access';import {db} from '../../../lib/db';
export default async function IntegrationEvents(){const a=await requireAdminPermission('settings');if(!a)redirect('/admin/login');const events=await db.integrationEvent.findMany({orderBy:{createdAt:'desc'},take:100});return <main className="min-h-screen bg-slate-100"><AdminNav active="integrations"/><div className="container py-7"><div className="flex justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-widest text-violet-400">System</p><h1 className="mt-1 text-3xl font-black">Integration Events</h1><p className="mt-2 text-sm text-slate-400">Operational history for Shopify, payments and other external services.</p></div><Link href="/admin/system-health" className="rounded-xl border border-white/10 px-4 py-2 text-sm font-bold">System health</Link></div><div className="mt-6 overflow-hidden rounded-2xl bg-white"><div className="divide-y divide-white/10">{events.map(e=><div key={e.id} className="flex flex-wrap items-center justify-between gap-3 p-4"><div><p className="font-black">{e.provider} · {e.eventType}</p><p className="mt-1 text-xs text-slate-500">{e.message||'No message'} · {e.createdAt.toLocaleString('en-IN')}</p></div><span className={`rounded-full px-3 py-1 text-xs font-black ${e.status==='SUCCESS'?'bg-emerald-400/10 text-emerald-300':'bg-red-400/10 text-red-300'}`}>{e.status}</span></div>)}{!events.length&&<p className="p-8 text-center text-slate-500">No integration events recorded yet.</p>}</div></div></div></main>}