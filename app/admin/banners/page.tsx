'use client';
import {useEffect,useState} from 'react';
import Link from 'next/link';
export default function Banners(){const [items,setItems]=useState<any[]>([]);const [form,setForm]=useState({title:'',subtitle:'',imageUrl:'',linkUrl:'/products',buttonText:'Shop now',sortOrder:'0'});const load=()=>fetch('/api/admin/banners').then(r=>r.json()).then(j=>setItems(j.banners||[]));useEffect(()=>{load()},[]);
const create=async()=>{const r=await fetch('/api/admin/banners',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const j=await r.json();if(!r.ok)return alert(j.error);setForm({...form,title:'',subtitle:'',imageUrl:''});load()};
const toggle=async(b:any)=>{await fetch('/api/admin/banners',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:b.id,enabled:!b.enabled})});load()};
const remove=async(id:string)=>{if(confirm('Delete this banner?')){await fetch('/api/admin/banners',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});load()}};
return <main className="min-h-screen bg-slate-100"><div className="container py-8"><div className="flex justify-between"><div><p className="text-sm text-fuchsia-600 font-bold">Storefront</p><h1 className="text-3xl font-black">Homepage banners</h1></div><Link href="/admin/dashboard" className="font-semibold">← Dashboard</Link></div><section className="mt-6 rounded-2xl bg-white p-5 shadow-sm"><h2 className="font-black">Add banner</h2><p className="mt-1 text-sm text-slate-500">Use an approved image URL or leave it blank for a gradient banner.</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><input placeholder="Title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} className="rounded-xl border p-3"/><input placeholder="Subtitle" value={form.subtitle} onChange={e=>setForm({...form,subtitle:e.target.value})} className="rounded-xl border p-3"/><input placeholder="Image URL" value={form.imageUrl} onChange={e=>setForm({...form,imageUrl:e.target.value})} className="rounded-xl border p-3"/><input placeholder="Link URL" value={form.linkUrl} onChange={e=>setForm({...form,linkUrl:e.target.value})} className="rounded-xl border p-3"/><input placeholder="Button text" value={form.buttonText} onChange={e=>setForm({...form,buttonText:e.target.value})} className="rounded-xl border p-3"/><input placeholder="Sort order" value={form.sortOrder} onChange={e=>setForm({...form,sortOrder:e.target.value})} className="rounded-xl border p-3"/><button onClick={create} className="rounded-xl bg-slate-900 p-3 font-bold text-white sm:col-span-2">Add banner</button></div></section><section className="mt-6 space-y-3">{items.map(b=><article key={b.id} className="flex flex-wrap items-center justify-between gap-4 rounded-2xl bg-white p-5 shadow-sm"><div><h2 className="font-black">{b.title}</h2><p className="text-sm text-slate-500">{b.subtitle||'No subtitle'} · order {b.sortOrder}</p></div><div className="flex gap-2"><button onClick={()=>toggle(b)} className="rounded-xl border px-3 py-2 text-sm font-bold">{b.enabled?'Disable':'Enable'}</button><button onClick={()=>remove(b.id)} className="rounded-xl border border-red-200 bg-red-50 px-3 py-2 text-sm font-bold text-red-700">Delete</button></div></article>)}</section></div></main>;}