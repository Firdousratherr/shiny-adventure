'use client'; import {useEffect,useState} from 'react'; import AdminNav from '../../../components/admin-nav';
export default function Suppliers(){const [items,setItems]=useState<any[]>([]);const [form,setForm]=useState({name:'',website:'',contactName:'',contactEmail:'',contactPhone:'',notes:''});const [busy,setBusy]=useState(false);const load=()=>fetch('/api/admin/suppliers').then(r=>r.json()).then(j=>setItems(j.suppliers||[]));useEffect(()=>{load()},[]);
const create=async()=>{if(!form.name.trim())return;setBusy(true);const r=await fetch('/api/admin/suppliers',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const j=await r.json();setBusy(false);if(!r.ok)return alert(j.error);setForm({name:'',website:'',contactName:'',contactEmail:'',contactPhone:'',notes:''});load()};
return <main className="min-h-screen bg-slate-100"><AdminNav active="suppliers"/><div className="container py-7"><div><p className="text-xs font-black uppercase tracking-widest text-violet-400">Channels</p><h1 className="mt-1 text-3xl font-black">Suppliers</h1><p className="mt-2 text-sm text-slate-400">Central supplier directory for sourcing, pricing and availability.</p></div><section className="mt-6 rounded-2xl bg-white p-5"><h2 className="font-black">Add supplier</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><input placeholder="Supplier name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="rounded-xl border p-3"/><input placeholder="Website" value={form.website} onChange={e=>setForm({...form,website:e.target.value})} className="rounded-xl border p-3"/><input placeholder="Contact name" value={form.contactName} onChange={e=>setForm({...form,contactName:e.target.value})} className="rounded-xl border p-3"/><input placeholder="Email" value={form.contactEmail} onChange={e=>setForm({...form,contactEmail:e.target.value})} className="rounded-xl border p-3"/><input placeholder="Phone" value={form.contactPhone} onChange={e=>setForm({...form,contactPhone:e.target.value})} className="rounded-xl border p-3"/><button disabled={busy} onClick={create} className="rounded-xl bg-slate-900 p-3 font-black text-white">Add supplier</button></div></section><div className="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{items.map(s=><article key={s.id} className="rounded-2xl bg-white p-5 shadow-sm"><div className="flex justify-between gap-3"><div><h2 className="font-black">{s.name}</h2><p className="mt-1 text-xs text-slate-500">{s.website||'No website'}</p></div><span className="zenvora-pill px-2 py-1 text-[10px] font-black">{s.status}</span></div><div className="mt-4 grid grid-cols-2 gap-2 text-xs text-slate-500"><span>Products <b className="text-white">{s._count.products}</b></span><span>Contact <b className="text-white">{s.contactName||'—'}</b></span></div></article>)}</div></div></main>}