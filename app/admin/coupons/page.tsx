'use client';
import {useEffect,useState} from 'react';
import Link from 'next/link';
export default function Coupons(){const [coupons,setCoupons]=useState<any[]>([]);const [form,setForm]=useState({code:'',type:'PERCENT',value:'10',minOrderAmount:'',maxDiscount:'',usageLimit:'',expiresAt:''});const load=()=>fetch('/api/admin/coupons').then(r=>r.json()).then(j=>setCoupons(j.coupons||[]));useEffect(()=>{load()},[]);
const create=async()=>{const r=await fetch('/api/admin/coupons',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const j=await r.json();if(!r.ok)return alert(j.error);setForm({...form,code:''});load()};
const toggle=async(c:any)=>{await fetch('/api/admin/coupons',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:c.id,enabled:!c.enabled})});load()};
const remove=async(id:string)=>{if(!confirm('Delete this coupon?'))return;await fetch('/api/admin/coupons',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});load()};
return <main className="min-h-screen bg-slate-100"><div className="container py-8"><div className="flex justify-between"><div><p className="text-sm text-indigo-600 font-bold">Promotions</p><h1 className="text-3xl font-black">Coupons</h1></div><Link href="/admin/dashboard" className="font-semibold">← Dashboard</Link></div><section className="mt-6 rounded-2xl bg-white p-5 shadow-sm"><h2 className="font-black">Create coupon</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><input placeholder="Code e.g. ZENVORA10" value={form.code} onChange={e=>setForm({...form,code:e.target.value})} className="rounded-xl border p-3"/><select value={form.type} onChange={e=>setForm({...form,type:e.target.value})} className="rounded-xl border p-3"><option>PERCENT</option><option>FIXED</option></select><input placeholder="Value" value={form.value} onChange={e=>setForm({...form,value:e.target.value})} className="rounded-xl border p-3"/><input placeholder="Minimum order" value={form.minOrderAmount} onChange={e=>setForm({...form,minOrderAmount:e.target.value})} className="rounded-xl border p-3"/><input placeholder="Max discount" value={form.maxDiscount} onChange={e=>setForm({...form,maxDiscount:e.target.value})} className="rounded-xl border p-3"/><input placeholder="Usage limit" value={form.usageLimit} onChange={e=>setForm({...form,usageLimit:e.target.value})} className="rounded-xl border p-3"/><input type="datetime-local" value={form.expiresAt} onChange={e=>setForm({...form,expiresAt:e.target.value})} className="rounded-xl border p-3"/><button onClick={create} className="rounded-xl bg-slate-900 p-3 font-bold text-white">Create coupon</button></div></section><section className="mt-6 space-y-3">{coupons.map(c=><article key={c.id} className="flex flex-wrap items-center justify-between gap-4 rounded-2xl bg-white p-5 shadow-sm"><div><h2 className="font-black">{c.code}</h2><p className="text-sm text-slate-500">{c.type==='PERCENT'?c.value+'%':'₹'+c.value} · used {c.usedCount}{c.usageLimit?' / '+c.usageLimit:''}{c.expiresAt?' · expires '+new Date(c.expiresAt).toLocaleString('en-IN'):''}</p></div><div className="flex gap-2"><button onClick={()=>toggle(c)} className="rounded-xl border px-3 py-2 text-sm font-bold">{c.enabled?'Disable':'Enable'}</button><button onClick={()=>remove(c.id)} className="rounded-xl border border-red-200 bg-red-50 px-3 py-2 text-sm font-bold text-red-700">Delete</button></div></article>)}</section></div></main>;}