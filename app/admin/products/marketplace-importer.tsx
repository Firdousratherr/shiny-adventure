'use client';
import {useState} from 'react';
export default function MarketplaceImporter(){
 const [url,setUrl]=useState(''); const [markup,setMarkup]=useState('30'); const [busy,setBusy]=useState(false); const [error,setError]=useState(''); const [preview,setPreview]=useState<any>(null);
 const previewProduct=async()=>{setBusy(true);setError('');setPreview(null);try{const r=await fetch('/api/admin/products/import-marketplace',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'preview',sourceUrl:url,markupPercent:Number(markup)})});const j=await r.json();if(!r.ok)throw new Error(j.error);setPreview(j.product)}catch(e){setError(e instanceof Error?e.message:'Unable to preview product.')}finally{setBusy(false)}};
 const importProduct=async()=>{setBusy(true);setError('');try{const r=await fetch('/api/admin/products/import-marketplace',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'import',sourceUrl:url,markupPercent:Number(markup)})});const j=await r.json();if(!r.ok)throw new Error(j.error);alert('Imported as DRAFT. Product ID: '+j.productId);location.reload()}catch(e){setError(e instanceof Error?e.message:'Unable to import product.')}finally{setBusy(false)}};
 return <section className="rounded-2xl bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-sm font-bold text-indigo-600">Marketplace importer</p><h2 className="text-xl font-black">Amazon & Flipkart</h2><p className="mt-1 text-sm text-slate-500">Paste a product URL, preview the source price, apply your markup, then import it as a draft.</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold">Official APIs</span></div><div className="mt-4 grid gap-3 md:grid-cols-[1fr_150px_auto]"><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="Amazon or Flipkart product URL" className="w-full rounded-xl border px-3 py-3" inputMode="url"/><input value={markup} onChange={e=>setMarkup(e.target.value)} placeholder="Markup %" className="w-full rounded-xl border px-3 py-3" inputMode="decimal"/><button disabled={busy||!url.trim()} onClick={previewProduct} className="rounded-xl bg-slate-900 px-5 py-3 font-bold text-white disabled:opacity-50">{busy?'Working…':'Preview product'}</button></div>{error&&<p className="mt-3 rounded-xl bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</p>}{preview&&<div className="mt-5 grid gap-4 rounded-2xl border bg-slate-50 p-4 md:grid-cols-[120px_1fr]">{preview.images?.[0]&&<img src={preview.images[0]} alt={preview.title} className="aspect-square w-full rounded-xl object-cover"/>}<div><h3 className="font-black">{preview.title}</h3><div className="mt-2 flex flex-wrap gap-2 text-sm"><span className="rounded-full bg-white px-3 py-1">{preview.provider==='AMAZON'?'Amazon':'Flipkart'} · Source ₹{Number(preview.sourceCost).toLocaleString('en-IN')}</span><span className="rounded-full bg-white px-3 py-1">Markup {preview.markupPercent}%</span><span className="rounded-full bg-white px-3 py-1 font-bold">Zenvora ₹{Number(preview.sellingPrice).toLocaleString('en-IN',{minimumFractionDigits:2})}</span></div><p className="mt-2 text-xs text-slate-500">Product will be created as DRAFT with stock 0. Images are intentionally not copied automatically unless the source API permits their reuse.</p><button disabled={busy} onClick={importProduct} className="mt-4 rounded-xl bg-indigo-600 px-5 py-3 font-bold text-white disabled:opacity-50">{busy?'Importing…':'Import to Zenvora'}</button></div></div>}</section>
}